from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A tool combining machine learning to detect ransomware viruses within a network.',
    author='Team RDM',
    license='MIT',
)
