language: python
python:
  - "3.8"
# command to install dependencies
install:
  - pip install -r ./requirements.txt
# command to run tests
script:
  - pylint ./src/data/kibana/
  - pylint ./src/data/scripts/
  - pylint ./src/data/utils/
  - ./travis_build.sh
